import { BookOpen, ChevronRight, Footprints, HeartPulse } from 'lucide-react'
const stages = ['BASE', 'CAPACITY', 'RETURN', 'RUNNER']
export function LearnScreen() { return <div className="screen-content"><div className="page-intro"><div><p className="eyebrow blue">THE READING ROOM</p><h1>Learn</h1><p className="muted">Know why the work matters.</p></div></div><section className="learn-feature"><BookOpen size={25}/><span className="eyebrow">READING ROOM</span><h2>Build the understanding.<br/>Then build the habit.</h2><p>Small, useful lessons for training with confidence.</p></section><section className="section-block"><div className="section-title"><div><span className="eyebrow">FOUNDATIONS</span><h3>Explore</h3></div></div><div className="learn-card"><div className="progress-icon"><HeartPulse size={20}/></div><div><h4>Anatomy</h4><p>The muscles and movements behind your sessions.</p></div><ChevronRight size={18}/></div></section><section className="section-block"><div className="section-title"><div><span className="eyebrow">LONG-TERM DIRECTION</span><h3>Road back to running</h3></div></div><div className="road-card"><div className="road-heading"><Footprints size={21}/><span>STAGE 01 / 04</span></div><div className="stage-track">{stages.map((stage, index) => <div className={`stage ${index === 0 ? 'selected' : ''}`} key={stage}><span>{String(index + 1).padStart(2, '0')}</span><strong>{stage}</strong></div>)}</div><p>Running is earned by evidence, not by the calendar.</p></div></section></div> }
